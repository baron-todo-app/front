/* eslint-disable @typescript-eslint/no-unused-vars */

import React from 'react'

// todo テスト不可
// act を使えば可能であるが、actがasyncに対応していない
// 16.9.0-alpha.0 は対応しているよう？, (react:16.8.6
// 結構調査しましたが、無理そうです すみません...

describe('-', () => {
  it('-', () => {
    expect(true).toBe(true)
  })
})
