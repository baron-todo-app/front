/* eslint-disable @typescript-eslint/no-unused-vars */

import React from 'react'
import { TaskUpsert } from '.'

describe('TaskUpsert', () => {
  it('存在確認', () => {
    expect(TaskUpsert).toBeDefined()
  })

  // 同等のテストを下記で知ている為 省略...
  // src/components/ecosystems/TasksOverview/index.test.tsx
})
