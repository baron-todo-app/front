/* eslint-disable @typescript-eslint/no-unused-vars */

import React from 'react'
import { UpsertForm } from '.'

describe('UpsertForm', () => {
  it('存在確認', () => {
    expect(UpsertForm).toBeDefined()
  })

  // 同等のテストを下記で知ている為 省略...
  // src/components/organisms/SearchList/index.test.tsx
})
