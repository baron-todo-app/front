/* eslint-disable @typescript-eslint/no-unused-vars */

import React from 'react'
import { TaskAdd } from '.'

describe('TaskAdd', () => {
  it('存在確認', () => {
    expect(TaskAdd).toBeDefined()
  })
})
